# newssummarizer
This project benchmarks **traditional extractive summarization** (LexRank) against a **state-of-the-art transformer model** (BART-large-CNN) using the CNN/DailyMail dataset.   A full evaluation is performed using **ROUGE-1, ROUGE-2, ROUGE-L** scores.
